---
name: Source Control brand (was clia-git)
description: The clia-git Mac app should be rebranded "Source Control" under wrkstrm, not clia
type: project
---

The Mac app currently named `clia-git` (sources at `private/universal/substrate/collectives/clia-app-org/private/apple/apps/clia-git/`) should be rebranded **Source Control** and assigned to **wrkstrm** rather than the clia family.

**Why:** It's a git client — "Source Control" is the natural product name. Belongs in the wrkstrm developer-tools family, not the clia consumer/agent family. Parallels the Prep Lab pattern (codename ≠ user-facing brand; entity assignment is durable and must be set before launch).

**How to apply:** When discussing or modifying this app, refer to it as Source Control (wrkstrm) externally. Expect future moves out of `clia-app-org`/`clia-org` collectives into a wrkstrm collective. Don't assign it to Laussat or rismay personal — it's wrkstrm Inc.
