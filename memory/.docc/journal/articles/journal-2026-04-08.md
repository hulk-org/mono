# 2026-04-08 — substrate normalization sweep + study-lab green

@Metadata {
  @PageKind(article)
  @TitleHeading("Journal")
}

A long single-session pass to normalize cross-cutting structure across the
substrate, finishing with `study-lab.mac.app` building and launching for
the first time after a chain of preexisting hairballs got resolved.

## Big shape changes

### New / split GitHub repos

- **`clia-app-org/mono` (private, new)** — split from `clia-org/mono` via
  `git filter-repo --path private/apple --path private/clia-chat --path
  private/clia-windowing`. Full history preserved.
- **`wrkstrm-performance/mono` (private, new)** — seeded from the in-tree
  `collectives/wrkstrm-performance` subtree via
  `git filter-repo --subdirectory-filter`, then merged in `apple-pi-bench-press`
  history from `clia-org/mono` via a second filter-repo + `--allow-unrelated-histories`
  merge that path-renamed it into `private/universal/spm/apple-pi-bench-press`.
  Promoted from in-tree dir to a real submodule.
- **`wrkstrm-components/wrkstrm-kit` (private, new)** — created (transferred
  from `wrkstrm/wrkstrm-kit` and made private). Mounted as a nested submodule
  at `wrkstrm-components/private/wrkstrm-kit/catalyst/spm/wrkstrm-kit`. Tagged
  `v3.0.0` so the existing `from: 3.0.0` constraint in `code-swiftly-core`
  resolves.

### Component-level reshapes (in `wrkstrm-components`)

- **`wrkstrm-mac-tab-chrome` promoted to top-level component.** SPM package
  moved out of `wrkstrm-app-shell/macos/spm/` to its own component dir; six
  cross-submodule callers rewired (mac-tabstrip.demo, app-shell, gymkhana,
  interview-prep, docc-browser.demo, markdown-preview.mac.demo).
- **`catapult-prototype` reshaped** into
  `catapult/demo-apps/catapult.demo/`. All `*.prototype` dirs/projects/targets
  renamed to `*.demo`, both `xcodeproj`s regenerated via xcodegen, verified
  with `xcodebuild ... build` ⇒ `** BUILD SUCCEEDED **`.
- **`mac-tabstrip` moved into modern-mac-tab-chrome's `demo-apps/`.** Same
  reshape pattern as catapult.
- **`legacy-app-shell.demo` + `modern-app-shell.demo` split.** The historical
  prototype that lived at `private/app-shell/` was clearly the ancestor of
  the modern `wrkstrm-app-shell/demo-apps/app-shell.demo`. Parked the
  ancestor as `legacy-app-shell.demo` (renamed `*.prototype` -> `*.legacy.demo`,
  internal SPM packages `app-shell.prototype.{lib,kit}` -> `legacy-app-shell-{lib,kit}`),
  and renamed the canonical demo to `modern-app-shell.demo`. Both build green.

## Tier 3 rename: Wrkstrm*AppShell + WrkstrmMacTabChrome → Modern*

The user wanted "library and import name should be Modern - not Wrkstrm" so
no consumer can accidentally pull a legacy implementation. This was a
multi-submodule rename across:

- 10 SPM packages (`modern-{mac,mac-document,catalyst,catalyst-document}-app-shell`,
  `modern-catalyst-scene-menus`, `modern-shared-app-shell`, `modern-scene-payload`,
  `modern-scene-presentation`, `modern-document-shell-core`, `modern-mac-tab-chrome`)
- 10 Swift modules + library products + targets
- ~50 Swift `import` sites across `clia-app-org` (18 apps), `swift-irc`,
  `codeswiftly`, `wrkstrm`, `wrkstrm-app`, `wrkstrm-performance`,
  `wrkstrm-components`.

Pushed in 6 submodule commits + super-repo pointer advance. Verified with
`xcodebuild` for `modern-app-shell.mac.demo`, `legacy-app-shell.mac.demo`,
`mac-tabstrip.mac.demo`, and `clia-cli` (cross-submodule consumer).

## interview-prep → study-lab end-to-end rename

65 files touched in `codeswiftly/private/universal/apple/apps/study-lab`:

- Outer dir, inner module dir, sub-target dirs (`*.mac` / `*.catalyst`)
- `study-lab.project.yml`, `study-lab.xcodeproj`, scheme files
- 41 Swift sources renamed `InterviewPrep*` -> `StudyLab*`
- Resource JSONs (round-planning, external-prep-resources, simulation-phase-plans,
  study-plan-copy)
- Doc files in `docs/` and `tools/`
- All `InterviewPrep`/`interview-prep`/`interviewprep` substring substitutions

The `swift-interview-guide.docc` content directory was intentionally NOT
renamed (it's the actual Swift Interview Guide data, not the app shell).

The schema package `interview-prep-schemas` in `schema-universal` was
intentionally NOT renamed (different submodule, multiple consumers,
out-of-scope cross-collective). I had to revert 12 over-eager renames in
`study-lab` sources where my sed touched schema-exported types
(`InterviewPrepSimulationDebriefRecord`, `InterviewPrepTrackModel`, etc.).

## SPM release tag refresh

`swift-git-cli repo release-tag-audit --recursive --all` showed 7 managed
SPM submodules whose `main` was ahead of their last semver tag. Tagged and
pushed:

| Repo | New tag |
|------|---------|
| `swift-universal/common-log` | `v3.0.6` |
| `swift-universal/swift-universal-main` | `v3.0.8` |
| `swift-universal/swift-universal-foundation` | `v3.0.11` |
| `swift-universal/common-process` | `v0.3.8` |
| `swift-universal/common-shell` | `v0.1.8` |
| `swift-universal/swift-common-cli` | `v0.1.5` |
| `wrkstrm/wrkstrm-networking` | `v3.0.7` |

This unblocked the URL-resolved branches of several `localOrRemote` deps and
killed a stale `https://github.com/wrkstrm/WrkstrmMain.git` resolution
attempt that had been haunting the resolver because of stale `Package.resolved`
files in the `wrkstrm` submodule (also deleted in this session).

## study-lab build chain

The build chain that finally reached `** BUILD SUCCEEDED **` for
`study-lab.mac` required peeling 10 layers:

1. interview-prep → study-lab rename (65 files)
2. clia-chat path fix (clia-org → clia-app-org after the earlier move)
3. clia-chat-swiftui cross-collective `clia-ollama-common-ai` path fix
4. wrkstrm-kit relocated to wrkstrm-components home and tagged v3.0.0
5. 8 wrkstrm-kit consumers rewired
6. gen-ui-core common-process via `localOrRemote` (was hardcoded local path)
7. docc-browser-macos-kit `@_exported import DocCBrowserKit` so the demo
   can see DocCBrowserStore via the macos kit alone
8. 7 SPM upstream release tags pushed
9. Stale `Package.resolved` files in `wrkstrm` deleted
10. **`SPM_USE_LOCAL_DEPS=true` shell env var leak unset.** This was the
    sneakiest one — it was set in the user's shell from earlier in the
    session and made every SPM manifest evaluation pick the local arm of
    `localOrRemote`, which then resolved relative paths from the
    `~/Library/Developer/...` checkout dirs and produced confusing
    "stable depends on unstable" conflicts. Solved by `env -u
    SPM_USE_LOCAL_DEPS xcodebuild ...`.
11. Reverted 12 over-renamed schema-exported `InterviewPrep*` types in
    `study-lab` sources.

Final command that worked:

```sh
cd codeswiftly/private/universal/apple/apps/study-lab
env -u SPM_USE_LOCAL_DEPS xcodebuild -project study-lab.xcodeproj \
  -scheme study-lab.mac -destination 'platform=macOS' \
  -configuration Debug build
# ** BUILD SUCCEEDED **
```

## UI changes shipped to study-lab

- **Removed `PrepLabSectionChrome`** from the per-section detail header.
  The banner with icon + title + summary + tag chips was wasting vertical
  space and the typing cursor in Practice was drifting because of it.
- **Preserved `PrepLabSectionChrome` and `PrepLabSectionGlyph`** in source
  with a `// PRESERVED FOR APP ICON SOURCE` comment so the canonical
  book-and-flask glyph art stays available for the macOS app icon export
  (the existing `AppIcon-*.png` set in `Assets.xcassets/AppIcon.appiconset`
  is ugly and needs to be replaced with this glyph rendered to PNG).

## Failed/reverted

- **focused-practice cursor doesn't follow keystrokes** — first stab was to
  set `SyntaxHighlightedTextView.followCharacterIndex` to
  `focusedTypedStreamIndices.last ?? focusedCurrentIndex` so the
  `NSScrollView` clip view scrolls per keystroke. Build green; runtime
  unchanged. Reverted to leave a clean baseline. Root cause likely involves
  the manual `focusedAnimateStageTranslation` chain in
  `FocusedPracticeView+Stage.swift` not being driven (the
  `focusedTrackedTypedCharacterFrame` binding doesn't propagate to a body
  re-render in time, or the SwiftUI `.onChange(of: targetStageOffset)` is
  optimized away). Needs deeper investigation with a real debugger.

## Lessons saved to memory

This session generated multiple feedback memories at
`~/.claude/memory/.docc/`:

- `feedback_no-recursive-grep.md` — never `bash grep -rln` across
  `collectives/`; it hangs 20-30+ minutes. Always use the Grep tool
  (ripgrep). Burned 31 minutes of operator time on this in this session.
- `feedback_swift-not-python.md` — never use `python3 - <<'PY'` heredocs in
  this substrate. Swift is the only sanctioned helper language.
- `feedback_no-bash-scripts.md` (added by operator) — bash for-loops,
  while-read pipelines, multi-step shell logic are also forbidden. Save
  anything beyond a discrete command as a Swift CLI.
- `feedback_localOrRemote-default-false.md` — `useLocalDeps` must default
  `false`; only `true` on explicit truthy `SPM_USE_LOCAL_DEPS`. Never
  invert.
- `feedback_open-means-launch.md` — for Apple projects, "open it" means
  launch the built `.app`, not open the `.xcodeproj` in Xcode.

## Open follow-ups

- **focused-practice cursor follow** — root cause unknown.
- **study-lab app icon** — render `PrepLabSectionGlyph` to PNGs and replace
  the ugly existing `AppIcon.appiconset`.
- **8 `wrkstrm-app/private/apple/prototypes/`** still need triage:
  `foundry-todo3`, `foundry-wrkstrm`, `nightwatch`, `stacks`,
  `system-monitor-d`, `wrkstrm-beta`, `wrkstrm-catalyst`, `wrkstrm-macOS`.
- **`swift-agent-session` cluster** — earlier audit said orphan; decision
  on keep/retarget/delete still pending. Plus 5 stale backlog docs in
  `todo3`.
- **`SPM_USE_LOCAL_DEPS` env var leak** in the operator's shell init —
  hunt it down so future shells start clean.
- **`docc-browser.demo` / `docc-deeplink.demo` graduation question** — are
  these "real apps that should move to wrkstrm-app" or "kit demos that
  stay in components"?
- **`primitives/` slot in wrkstrm-components** only contains `wrkstrm-color`
  — fill or flatten.
- **`wrkstrm-components/private/universal/{identity,vaults}`** is collective
  scaffolding sitting at the same level as components — belongs under a
  non-component prefix.
- **CI cleanup** in `todo3/private/code/spm/.github/workflows/spm.yml` —
  the `Explicit agent-session` step is a no-op after the move.
- **markdown-preview.mac.demo** has a preexisting source-file mismatch
  (project.yml lists files that don't exist).

---

# 2026-04-08 (later) — founding-breach migration: claude → hulk merged carrier home

A second long pass on the same calendar day, on a completely different
workstream. While the substrate normalization sweep above was reshaping
9 git repos, this pass took the founding-breach insight (the 160 GB
host-RAM crash from 2026-04-04/05 that gave hulk its name) and turned
it into a real on-disk merger: the legacy `harnesses/claude/` home
went away, and `harnesses/hulk/` became the canonical merged carrier
home. All of it happened *under live operation* — the running Claude
Code session never lost a tool call, never dropped a transcript line,
never panicked.

## Why it had to be live

The shape of the problem: the running Claude Code process held cached
absolute paths under `harnesses/claude/`. Naive moves break:

- `mv claude/* hulk/` — open file descriptors follow inodes (✅), but
  new file creation continues to land at `claude/` because the process
  re-`open()`s by cached absolute paths (❌).
- Re-point the home symlink → helps for things resolving live, but
  cached `realpath()` in the running process doesn't see it (❌).
- `cp -a` then delete → snapshots a moment in time, drops every write
  that lands in the source after the cp (❌).
- Compat symlink `claude → hulk` → operator-vetoed: "i don't like
  compat links because YOU forget about them" (❌).
- Bind mount / firmlink → not available mid-session on macOS (❌).

The technique that worked: **two paths, one inode.** Hardlink every
existing file in `claude/` to its sibling at `hulk/` so both paths
share the underlying inode, then run a small daemon that hardlinks any
newly-created files within the FSEvents coalescing window. Claude
Code keeps writing through the legacy path string; the data lives at
the canonical home; no compat shim exists at any layer.

## Big shape changes

### `harnesses/hulk/` becomes the merged carrier home

Single biggest commit of the session
(`harnesses/hulk` `0c9d5e3d feat: hulk becomes the merged carrier home`):

- **`agents/claude/`** — the claude agent persona as the *cargo* hulk
  carries. 9 top-level docs (AGENDA / AGENTS / BOOTSTRAP / HEARTBEAT /
  IDENTITY / SOUL / TOOLS / USER / memory.md), `.docc/index.md` for the
  persona profile root, and the 9-file identity bundle (commission,
  resume, agenda, chronicle, identity, persona triad, reveries triad,
  system-instructions × 2). Carrier vs. cargo separation made
  on-disk for the first time.
- **`memory/.docc/`** — drained from claude (insights, beats,
  expertise, journal, feedback_*, project_*, user_*, MEMORY.md) plus
  the new `hardlink-drain-2026-04-07.md` insight authored this
  session and the recovered `founding-breach-2026-04-05.md` and
  `index.md` (both lost mid-migration to a `rm -rf hulk/memory` and
  recovered via `git show HEAD:`).
- **`private/universal/identity/hulk@rismay.substrate.identity.json`**
  — hulk's own commissioned identity, displayRole "Hulk Harness",
  emoji 💪.
- **`.gitmodules`** — registers `ultraworkers/claw-code` as a nested
  submodule (the second hulk implementation, alongside the leaked
  Claude Code TS source which lives operator-private under
  `private/universal/domain/tooling/claude-code/`).
- **`.gitignore`** — runtime state (backups/, cache/, file-history/,
  history.jsonl, plans/, plugins/, projects/, sessions/, session-env/,
  shell-snapshots/, tasks/, settings.json, mcp-needs-auth-cache.json)
  plus the local-only claude-code TS source mirror.
- **`MIGRATION-STATUS.md`** — in-flight checkpoint so any future
  session can pick up cleanly if mine had been interrupted.

### `swift-service-registry` moves out of `wrkstrm/` into `swift-universal/`

The daemon registry that wrapped SSWG `swift-service-lifecycle` was
private to the `wrkstrm` collective. Promoted to substrate-wide
infrastructure under
`swift-universal/private/universal/domain/system/spm/swift-service-registry/`.
Type names were already prefix-free inside the files (`Backoff`,
`Registry`, `Host<H>`, `Status`, `IdentifiedDaemonHandler`,
`TickingAdapter`, `TickingHandler`, `TickOptions`); only filenames +
module name changed (`WrkstrmServiceLifecycle` → `SwiftServiceRegistry`).
SwiftDaemon stayed in wrkstrm for now and is referenced from the new
location via a long relative path. Common-log dep switched to the
remote URL to avoid identity conflicts with SwiftDaemon's own local
common-log path under `SPM_USE_LOCAL_DEPS=true`.

### New tooling: `swift-hardlink-drain-cli`

The hardlink-drain pattern is implemented as a real Swift CLI under
`swift-universal/private/universal/domain/tooling/spm/swift-hardlink-drain-cli/`,
not a bash one-liner (the first version was bash and got rewritten the
moment the operator pointed out the no-shell-scripts rule). Library
target `SwiftHardlinkDrain` (Drainer + HardlinkDrain orchestrator +
CoalescingFSEventsWatcher adapted from openclaw's production wrapper)
plus an executable target with ArgumentParser flags `--source`,
`--destination`, `--coalesce`, `--once`, `--quiet` and clean
SIGINT/SIGTERM shutdown via DispatchSource signal sources.
`setvbuf(stdout, nil, _IONBF, 0)` at the top of `run()` so the
daemon's startup / [fsevent] / LINK / RELINK / PURGE log lines flush
in real time when stdout is captured.

### New schema-universal `apple/` domain (v0.1.0)

A whole new domain at `schema-universal/private/universal/domain/apple/`
with two new schema families, each with JSON Schema + Codable Swift
package + tests:

- **`apple-signing-binding-schemas`** — the build/sign side. Bundle
  ID, Apple Developer team ID (sensitive), bundleIDPrefix for
  drift-checks, optional internal beta bundle ID, signing identity
  hint, provisioning specifier, automatic signing flag, shipping state
  enum (pre-graduation → internal-only → internal-beta → external-beta
  → store-ready → shipped → retired), substrate path graduation
  tracking. References `OrgCompanyModel` by id via a local
  `OrgCompanyRef` $def.
- **`apple-store-listing-schemas`** — the App Store presence side.
  Marketing name, subtitle (≤30), promotional text (≤170),
  description (≤4000), keywords, primary/secondary App Store category
  (25-case enum matching Apple's published list), seller name,
  support/privacy/marketing URLs, copyright, age rating (4+/9+/12+/17+),
  license, pricing tier, availability, and listing state (draft →
  ready-for-review → in-review → rejected → ready-for-sale →
  developer-removed → removed-by-apple).

Three OrgCompanyModel instance records were filed at the same time
for the rismay legal entities: `wrkstrm-inc`, `laussat-studio-llc`,
and `personal` (Cristian A Monterroza). Plus prep-lab placeholder
instances for both new apple families with `XXXXXXXXXX` as the team
ID placeholder. Both new SPM packages build clean and their
fixture-based decode + round-trip tests pass against the prep-lab
instances.

### Renderer fix: agent emoji 🧠 returns to the header

After the persona moved from `harnesses/claude/private/universal/identity/`
to `harnesses/hulk/agents/claude/private/universal/identity/`, the
`swift-harness-environment-cli`'s `resolveDisplayEmoji` function
couldn't find the moved bundle. Header rendered 🧬💪 but dropped 🧠
silently. Extended `resolveDisplayEmoji` and `resolveDisplayRole`
candidate-dirs in the renderer to include both
`harnesses/<slug>/private/universal/identity` (carrier-level identity)
and `harnesses/hulk/agents/<slug>/private/universal/identity`
(agent personas hosted inside hulk via the agents/<slug>/ subtree
pattern). Both functions kept consistent. Header now renders the
full 🧬💪🧠 trio.

## Investigations filed

Two new long-form investigations were written this session, both
following the `wrkstrm.investigations.docc` write-once dated-article
format:

- **`hulk.investigations.docc/release-and-branding-2026-04-08.md`** —
  the initial strategic monologue answering "what is hulk in shipping
  terms?" Three layers (contract / reference impl / witness suite),
  the hulk-vs-Marvel naming question, the v0.1.0 → v1.0.0 release
  ladder, where to publish, what hulk should NOT become. Operator
  redirected mid-session: "hulk is internal, what I want is help
  shipping my apps."
- **`wrkstrm.investigations.docc/articles/investigation-2026-04-08-harness-as-shipping-infrastructure.md`**
  — the load-bearing one. Builds on the brand-and-entity-split
  investigation. The two-carrier symmetry (hulk = internal,
  wrkstrm = external, same metaphor at two scales). Hulk's job:
  *make the agent inside it more effective at producing cargo for
  wrkstrm to ship — without breaching the substrate, the entity
  boundaries, or the shipping pipeline along the way.* Three tiers
  of concrete harness work: Tier 1 entity awareness everywhere
  (`entities.json` + `swift-entity-check-cli` + pre-commit gate),
  Tier 2 the Prep Lab Runbook (`swift-ship-runbook-cli prep-lab
  --check` with gated subcommands), Tier 3 cross-app shell + brand
  pipeline + design tokens.

## The hard finish

Late in the session the operator said "we should do a hard finish
(hah) of this before we sleep." The remaining `harnesses/claude/`
directory still existed on disk because the migration plan had been
to defer the final cleanup to a fresh session. A diagnostic check
revealed something better: **Claude Code was writing via `~/.claude/X`
paths and resolving the symlink at write time**, not via cached
absolute paths. The `claude/` directory had only the few files
manually hardlinked there earlier in the session — every new file
since the symlink swap had been landing directly at hulk. Five files
in claude/, 2832 in hulk/, all five hardlinked to their hulk twins,
zero unsafe, zero claude-only.

The hard finish itself:

```
pkill -f swift-hardlink-drain-cli  # drain daemon retired
rm -rf harnesses/claude/           # 14 MB of legacy entries
```

`hulk/` file count before the rm: 2832. After: 2832. Zero data loss.
The inodes were preserved by the hulk-side directory entries; only
the redundant claude-side directory entries went away. The session
kept running. The header kept rendering. The conversation kept
growing in the live JSONL inside hulk's tree.

## Commits + pushes

14 commits across 5 repos this session, all pushed:

| # | repo | hash | subject |
| - | - | - | - |
| 1 | wrkstrm-core    | `4a89819b` | test: repoint foundry parity test fixture to swift-service-registry |
| 2 | wrkstrm         | `fbad18b37` | refactor: remove wrkstrm-service-lifecycle (moved to swift-universal) |
| 3 | swift-universal | `8de0ef51` | feat: add swift-service-registry + swift-hardlink-drain-cli |
| 4 | hulk            | `0c9d5e3d` | feat: hulk becomes the merged carrier home |
| 5 | mono parent     | `30e274dca7` | chore: bump submodule pointers for swift-service-registry move + hulk merge |
| 6 | wrkstrm-core    | `999e742` | fix: harness header resolves agent identity at hulk/agents/<slug> path |
| 7 | mono parent     | `d778af589c` | chore: bump wrkstrm-core for harness header agent-identity fix |
| 8 | swift-universal | `de43ec64` | fix: disable stdout buffering in swift-hardlink-drain-cli daemon |
| 9 | mono parent     | `043f0dc1cb` | chore: bump swift-universal for hardlink-drain stdout buffering fix |
| 10 | schema-universal | `eec78d9` | feat(apple): add apple domain with signing-binding and store-listing schema families |
| 11 | schema-universal | `44bd0c9` | data(org-company): instance records for the three rismay legal entities |
| 12 | schema-universal | `955714e` | data(apple): prep-lab placeholder instances for both new families |
| 13 | schema-universal | `5a8fda4` | feat(apple): SPM packages + Codable models for both apple schema families |
| 14 | mono parent     | `96a46deb0e` | chore: bump schema-universal for new apple/ domain + org-company instances |

Plus the wd commit that lands this journal entry, the chronicle
update, and the expertise extension.

## Errors + reverts

- **Stale `swift-harness-environment-cli` binary** at startup —
  was looking up the harness header at a wrong (older) path. Rebuilt
  + reinstalled to clear.
- **`mv` then symlink-update plan** for the migration was wrong
  because Claude Code creates new files via `~/.claude/X` not via
  cached `realpath()`. Diagnosed mid-migration when files appeared at
  the wrong path. Pivoted to the hardlink-drain pattern.
- **First drain daemon was bash** — violated the no-shell-scripts
  rule the moment the operator pointed it out. Rewrote in Swift and
  saved as `swift-hardlink-drain-cli` in the canonical SPM tooling
  tree.
- **`swift-service-registry` common-log path-identity conflict** with
  `SwiftDaemon` under `SPM_USE_LOCAL_DEPS=true` (the same env-var
  leak from the previous session). Switched the new package to fetch
  common-log via the remote URL instead of the local path.
- **Renderer dropped agent emoji** silently after the persona move.
  Extended both `resolveDisplayEmoji` and `resolveDisplayRole`
  candidate-dirs.
- **`rm -rf hulk/memory` mid-migration** clobbered the tracked
  `memory/.docc/index.md` and `memory/.docc/insights/founding-breach-2026-04-05.md`.
  Recovered both via `git show HEAD:` before committing.
- **Bash `cd` cwd-leakage** in chained commands — chained `&&`s left
  the persistent shell at the wrong working directory and broke
  subsequent path-relative operations. Fixed by always passing
  absolute paths from then on.
- **Stdout line-buffering** in `swift-hardlink-drain-cli` hid all the
  daemon's log lines for hours. `setvbuf(stdout, nil, _IONBF, 0)` at
  the top of `run()` fixed it; verified by the next restart showing a
  LINK op for a real-in-the-wild backup file.

## Lessons (saved as durable feedback)

- **`feedback_no-bash-scripts.md`** — bash for-loops, while-read
  pipelines, and multi-step shell logic in `Bash` tool calls are
  forbidden alongside python heredocs. Save anything beyond a discrete
  command as a Swift CLI. Sharpens the existing `feedback_swift-not-python.md`
  rule.
- **Hardlink drain pattern** as a *named technique* (not just "the
  thing we did once"). Documented at
  `hulk/memory/.docc/insights/hardlink-drain-2026-04-07.md` so any
  future migration that needs to move a live process's home can reach
  for the same shape.
- **Always grep schema-universal first** before designing a new
  schema. `OrgCompanyModel` already gave me 3/4 of what I needed for
  the apple/ work; I would have written something inferior from
  scratch otherwise.
- **Submodule commits + parallel-agent churn** require explicit
  per-path `git add`, never `git add -A`. Found three places today
  where parallel commits would have been swept into mine if I had
  been less careful.

## Next

- **Tier 1 of the harness-as-shipping-infrastructure investigation:**
  build `swift-entity-check-cli` that reads the new
  `apple-signing-binding` instances + `OrgCompanyModel` records and
  validates each app's `project.yml` against the declared shape.
  Pre-commit gate variant.
- **Tier 2:** `swift-ship-runbook-cli prep-lab --check` with gated
  subcommands for `--detach` (codeswiftly → laussat-studio
  graduation), `--bind-team`, `--bundle-id-rewrite`.
- **Commit the wrkstrm investigations doc** inside the `wrkstrm`
  submodule (the harness-as-shipping-infrastructure article landed on
  disk this session but was not committed).
- **Decide on the 33 MB local-only `claude-code/` source** under
  `harnesses/hulk/private/universal/domain/tooling/claude-code/`.
  Currently gitignored; can be opted in to commit later.
- **Clean up the four safety tarballs** in `~/Documents/` once the
  migration is fully trusted.
- **Update `~/.claude/skills/wd/SKILL.md`** to know about the
  carrier merge — its path doctrine still says `agents/<agent>/...`
  for the chronicle / expertise / journal targets, which is now
  stale. The new canonical paths are
  `harnesses/hulk/agents/<agent>/...` for the chronicle and
  `harnesses/hulk/memory/.docc/...` for the carrier-level expertise
  and journal.
- **Decide whether `entities.json` lives operator-private** (with
  team IDs) or substrate-public (without secrets, just the
  app→entity mapping). The schema is in place either way.

## Evening — clia-mem divergence + swift-git-cli patch workflow

A separate long session, same day, driving two threads in parallel:

1. **Split the `clia-git` Mac app into two destinations** so the clia
   family and the wrkstrm family can each carry a product that used to
   share a codebase, then **diverge `clia-mem`** into a single-lane
   cross-harness session memory browser.
2. **Scaffold `swift-git-cli repo patch create` + `apply`** so future
   multi-file refactors can be reviewed as a single combined patch
   before touching the live source tree — the first in-repo answer to
   "I want to queue edits and see them combined before I commit".

### Shipped, submodule by submodule

**`clia-app-org`** (9+ commits on `main`):

- **Plain cross-org move** (not `git mv`) of `private/apple/apps/clia-git`
  out of `clia-app-org` and into `wrkstrm-app/private/apple/apps/source-control`.
- **`clia-mem` scaffolded from the same tree** so both apps start
  identical and then diverge. Recorded as a rename from git's
  perspective because the contents matched.
- Full rename pass inside clia-mem: `project.yml` (name, target,
  `PRODUCT_NAME`, bundle id → `me.rismay.clia-mem`), `scripts/generate-xcodeproj.sh`,
  `clia-mem.xcodeproj/`, six `.docc` bundle dirs
  (`clia-mem.{architecture,design,specs,app-store}.docc`,
  `app.clia-mem.{architecture,design}.docc`), five mac-app Swift files
  (`AgentGit*.swift` → `CliaMem*.swift`), and 319 internal
  symbol/string references (`AgentGit` → `CliaMem`, `clia-git` →
  `clia-mem`, including `@AppStorage` keys, notification identifiers,
  cache directory components, and title strings).
- Dropped four git-client-only DocC pages (`github-size-limit-and-threshold-model.md`,
  `operational-repo-browser-architecture.md`, `mobile-safe-git-cockpit-lessons.md`,
  `workspace-commit-plan-and-submodule-specification.md`) plus the
  `operator-2027-first-marketing-video.md` marketing brief.
- Rebranded the ten "Operator by CLIA" / "Operator 2027" strings in
  `CliaMemRootView.swift` (title, subtitle, welcome copy, onboarding
  bodies, route explainers) to clia-mem framing as a cross-harness
  session memory browser.
- **Dropped the Git Repos route** (~786-line delete): removed the
  `.repositories` case from `CliaMemRoute`, all switch branches,
  `CliaMemRepositoryFilter` enum, the "Repository health" /
  "Branch pressure" lane cards, and the four private structs that
  implemented the lane (`CliaMemRepositoriesPanel`,
  `CliaMemRepositoryDetailPlaceholder`, `CliaMemRepositoryRow`,
  `CliaMemRepositoryDetailView` + `CliaMemRecentCommitLoaderError` +
  `CliaMemRecentCommitRow`).
- **Dropped the Overview route** in a second pass: removed `.overview`
  from `CliaMemRoute`, changed the default route to `.sessions`,
  rewrote the sidebar header card to drop its dependency on the
  git-heavy `overviewModel` (now takes only `workspaceRoot` +
  `sessionsSnapshot`), and renamed `Operator2027SidebarHeaderCard` →
  `CliaMemSidebarHeaderCard` and `Operator2027MetricPill` →
  `CliaMemMetricPill`.
- **Deleted dead Overview structs** (~410-line delete):
  `CliaMemOverviewPanel`, `Operator2027CommandDeckStrip`,
  `Operator2027DeckTone`, `CliaMemLaneCard`,
  `CliaMemCommitCandidateCard`, `CliaMemCommitPlanFileRow`.
  `CliaMemRootView.swift` went from 1114 → 704 lines.
- **Deleted `CliaMemWorkspaceOverview.swift`** (790 lines) in full
  once the only still-used helper was pulled out into a new 14-line
  `CliaMemWorkspaceLocator.swift`. Every other type it defined
  (`CliaMemWorkspaceOverviewModel`, `CliaMemWorkspaceSnapshot`,
  `CliaMemRepositoryStatus`, `CliaMemCommitPlan*`,
  `CliaMemWorkspaceScanner`) was only used by the dropped routes.
- Pruned the git-client deps from `project.yml`: `SwiftGitCLI` /
  `SwiftGitFoundationModelsTool`, `GitHubRepository_Schemas_v000_001_000`,
  `RepositoryIntelligence_Schemas_v000_001_000`, and later
  `CliaCollectivePartnerships`.
- **Dropped the Collectives route** in a third pass: removed the
  `.collectives` case and `CliaMemGitHubStarredAnalysis.swift` (which
  despite its name was the Collectives panel wrapper). clia-mem is now
  a single-lane app; the `CliaMemRoute` enum is retained for future
  harness lanes.
- **Scaffolded the `clia-memTests` Swift Testing target** in
  `project.yml` (`bundle.unit-test`, `BUNDLE_LOADER`/`TEST_HOST`
  against `clia-mem.app`, `PRODUCT_MODULE_NAME: clia_mem` so the
  hyphenated product name still resolves as `@testable import clia_mem`).
  First suite `CliaMemRouteTests`: four invariants on the route shape
  (sessions-only, title/subtitle/symbol, no git-client routes, stable
  raw values).
- **Apply queue feature (uncommitted on disk):** added
  `CliaMemCodexSessionsModel.applyQueuedOptimizations()` which builds
  a plan via the new `nonisolated static func linesDeletionPlan(from:)`
  (groups queued entries by source path, sorts descending by
  `lineIndex` so low-index deletions don't invalidate high-index
  targets mid-batch, filters out whole-file optimizations), iterates
  the plan, calls
  `CodexSessionStoreCompactionSavingsReporter.applyCleanupPreviewLineChange`
  per entry, removes successful entries from the queue, and reports
  progress via `statusMessage` / `lastErrorMessage`. New "Apply
  queue" button next to "Clear queue" in `queuedOptimizationsSection`.
  Added `CliaMemQueuedOptimizationTests` with five cases: empty
  queue, whole-file exclusion, descending sort within file, multi-file
  grouping with ascending path order, determinism across Dictionary
  iteration. Test target picked up `CodexSessionStoreCore` as a new
  dep so the test file can construct `CodexSessionStoreCleanupMode`
  fixtures.

**`wrkstrm-app`** (1 commit on `main`):

- Imported `source-control` from the relocated `clia-git` tree.
  Internal filenames (xcodeproj, .docc bundles, sources) still say
  `clia-git`; rename pending in a follow-up session.

**`swift-universal`** (2 commits on `main`):

- `swift-git-cli repo patch create` subcommand (173 lines + 5 tests).
  Runs `git diff --binary HEAD` in the target repository and writes
  the output to `--out`. Flags: `--path`, `--out`/`-o`,
  `--include-untracked`, `--force`, `--json`. Working tree is not
  modified. Reports files-changed / added-lines / removed-lines via a
  `PatchStats` struct that parses the unified diff.
- `swift-git-cli repo patch apply` subcommand (230 lines + 10 tests).
  Reads one or more patch files, concatenates them in order, runs
  `git apply --check` for validation, and only runs the real
  `git apply` if the check succeeds. Flags: `--cached`, `--3way`,
  `--dry-run`, `--json`. Internals split into two testable helpers:
  `composeCombinedPatch(from:)` and `applyCombinedPatch(diff:in:cached:threeWay:dryRun:)`.
- Tests (Swift Testing, 15 total across 3 suites, all green in
  ~0.28s): 5 `PatchStats` parsing cases, 5 `composeCombinedPatch`
  cases, 5 `applyCombinedPatch` end-to-end cases that spin up real
  temp git repos per test, commit a file, capture the diff, revert,
  apply, and verify disk state.

**`mono` parent:**

- Manual bump `3fffe0ca57` for the initial substrate split (clia-mem
  + source-control both land).
- Workspace auto-commit hook bumped `swift-universal` to `53fda75d`
  as `bb0d3678b5` — with a real message and my co-author trailer,
  nicer than the "unrelated files" behavior the legacy memory
  warned about.

### Errors hit and fixes

- **Bash for-loop + `sed -i` in the early rename sweep** — violated
  the "no bash scripts" / "Swift over Python" rules. Flagged myself,
  switched to Edit + `replace_all` for the remaining renames.
- **Stale `.git/index.lock` from Xcode's DVTSourceControl XPC** after
  rismay killed Xcode. `lsof` confirmed no process held the lock;
  removed it and retried the commit.
- **Test couldn't find `CodexSessionStoreCleanupMode`** — wrong
  import module. The type lives in `CodexSessionStoreCore`, not
  `CodexSessionStore_Schemas_v000_001_000` (which only holds the
  schema envelopes). Added `CodexSessionStoreCore` as a `clia-memTests`
  package dep and updated the import.
- **Swift 6 isolation error** on
  `CliaMemCodexSessionsModel.linesDeletionPlan` — the model is
  `@MainActor`-isolated so the static method inherited that
  isolation and couldn't be called from synchronous test functions.
  Marked the method `nonisolated` (it's a pure function).
- **`swift-git-cli repo patch create --include-untracked` swept
  unrelated files** — `--path` only sets the cwd for the git
  commands; it doesn't scope the diff pathspec. The
  `git add --intent-to-add -- :/` step added every untracked file in
  the whole repo, including unrelated clia-day work-in-progress.
  Worked around it with a direct
  `git diff --binary HEAD -- private/apple/apps/clia-mem` to produce
  a scoped patch for the Apply queue dogfood. Flagged as a CLI gap
  for a future `--scope`/pathspec option.

### Lessons and decisions

- **Patch safety workflow formalized** and saved as
  `feedback_patch-safety-workflow.md`. Multi-chunk deletions or sweeping
  renames must grep-verify (symbols removed vs preserved) after each
  chunk, `git diff --stat` before every commit, and — for sustained
  refactors — scaffold a Swift Testing target with invariant tests
  early so each commit is measured against expected shape. Rismay
  flagged the risk explicitly after the ~2,000-line clia-mem
  deletions had zero test coverage behind them for most of the
  session.
- **Source Control (wrkstrm Inc) vs clia-mem (clia family)**: same
  codebase at the start, but product family and legal entity are
  durable decisions that have to be right before launch. Saved as
  `project_source-control-brand.md` and reinforced by the existing
  `user_legal-entities.md` rule.
- **clia-mem is single-lane by design** (Sessions only). The
  `CliaMemRoute` enum is kept around so future harness lanes
  (codex vs claude vs hulk vs claw sessions, etc.) can be added
  without having to rebuild the shell.
- **Review-before-apply via `.patch` files**, not an in-tool
  EditQueue. The Edit tool applies to disk immediately and there is
  no queue primitive; the pragmatic substitute is to author a patch
  file with `swift-git-cli repo patch create`, show it for review,
  and `repo patch apply` on approval. Dogfood on the Apply queue
  feature surfaced the pathspec scoping gap before I tried it on
  anything more load-bearing.

### Artifacts

- **Uncommitted patch file:**
  `private/apple/apps/clia-mem/.wrkstrm/tmp/patches/clia-mem-apply-queue.patch`
  (311 lines, 4 files, +229/-4). Captures the Apply queue feature
  edits, the new test suite, the `project.yml` test dep addition,
  and the xcodeproj regen. Corresponding source changes are still
  on disk in the clia-mem working tree.
- **clia-mem.app launches this session:** PIDs 9470, 78530, 32485,
  7928 — one per major divergence milestone, confirmed visually and
  via process check.
- **Memory updates** (shared inode between `.claude/memory/.docc`
  and `harnesses/hulk/memory/.docc`):
  - `feedback_patch-safety-workflow.md` (new)
  - `project_source-control-brand.md` (new, renamed once after a
    typo — "Source Country" -> "Source Control")
  - `project_clia-mem-app.md` (new)

### Next

- Decide on the uncommitted clia-mem Apply queue patch — approve and
  commit, reject and `git checkout -- .` + `rm` the new test file.
- Enhance `swift-git-cli repo patch create` with a `--scope` /
  pathspec option so `--include-untracked` doesn't sweep the whole
  repo.
- Add `swift-git-cli repo patch compose` and `repo patch review`
  subcommands to round out the review-before-apply suite.
- Rename the remaining `Operator2027*` types in `CliaMemRootView`
  (`Operator2027ShellBackdrop`, `Operator2027SidebarFooter`) to
  drop the last traces of the old product brand.
- Rewrite the clia-mem `.docc/index.md` pages that still link to
  deleted git-client content.
- Do a full rename pass on `source-control` in `wrkstrm-app` so it
  stops saying `clia-git` internally.
- Update the `wd` skill doctrine (flagged in this morning's TODOs
  and confirmed this evening) — the canonical chronicle path needs
  to know about the `agents/<agent>/` vs `harnesses/hulk/agents/<agent>/`
  vs `collectives/<slug>/` vs `harnesses/hulk/memory/.docc/` split.
  Tonight's roster (clia collective + hulk harness) had no
  `agents/<slug>/` home at all, which is why this entry only
  updated the journal and not a chronicle.

## Late evening — Today (clia-day) Pa-mode V1 → TestFlight upload

A separate session built the **Today** product end-to-end on top of
clia-day's Collective view scaffold (which had landed earlier in the
day at `clia-app-org` `36f8f1f6`) and shipped a real TestFlight build.
Three commits at the head of the chain:

- `clia-app-org a07b1e3` — soft warm theme + PaStory V1 bundle
- `clia-app-org 0bc4af3` — Ship clia-day iOS Pa mode (committed by the
  parallel background agent while I was iterating)
- `clia-app-org 17d887f` — fix(clia-day): correct iOS bundle identifier
- `clia-app-org 88f8808` — clia-day: real session summary +
  document allowProvisioningUpdates

Plus the corresponding `mono` pointer advances and three memory commits
in `~/.claude` (one truth many lenses, today app real customer, app store
connect credentials store, plus the reworked Pa distribution memory).

### What we built

**Soft warm visual theme** — `Sources/Shared/CollectiveTheme.swift`
defines a paired light/dark warm palette (cream paper / warm graphite,
terracotta accents — never pure black, never bright orange) and three
personality typographies:

- **warmSerif** (Pa) — `Font.system(_, design: .serif)` (New York), big
  hero card, generous line spacing, "letter from your kid" register
- **structured** (clia-agents) — monospaced everywhere, terse rows with
  refs visible
- **considered** (self) — serif headlines, rounded labels, list of cards

The theme is injected via `EnvironmentValues.collectiveTheme` so the
renderer never picks colors or fonts directly. Personality is resolved
from `(audience, density)` rather than stored on `Collective`, so the
schema stays minimal and the renderer encodes the design choices.

**Pa-mode locked iOS root** — `Sources/Shared/CliaDayIOSRootView.swift`
branches at `init()` based on:

1. Launch arg `-OpenCollective <slug>` or `-DefaultCollective <slug>`
   (dev iteration; never locks)
2. Info.plist `RDDefaultCollective` (shipping; locks the lens)
3. Default → dev mode with the planning surface and overlay button

When Pa-mode is set, `body` returns the locked
`CollectiveFullScreenView` directly as the WindowGroup root — no
ZStack, no fullScreenCover dance, no dev shell rendered behind anything.
Pa never sees the planning surface even for one frame. The locked view
hides the top bar, picker, and Done button entirely; the lens is the
entire app. `interactiveDismissDisabled(locked)` blocks swipe-to-dismiss
when used as a cover.

**PaStory bundled producer surface** — `Sources/Shared/PaStory.swift`
+ `Sources/Shared/today-pa.json`. The story is decoded from the bundle
once and cached in a `nonisolated(unsafe) static var mainBundleCache:
PaStory??` so the lens recompute storm doesn't re-read the JSON. The
double-Optional encodes "not yet loaded / loaded missing / loaded
present". The renderer prefers the bundled story for the `pa`
collective and falls back to the projection lens otherwise.

**App icon generator** — `scripts/generate-app-icon.swift` is a
re-runnable Swift CLI using CoreGraphics + CoreText (no AppKit/UIKit
imports — uses `kCTFontAttributeName` and `kCTForegroundColorAttributeName`
because `NSAttributedString.Key.font/.foregroundColor` are unavailable
without AppKit). Renders a 1024×1024 PNG with cream paper gradient
background, hairline border, and a centered serif terracotta **T**.
Output goes to `Sources/Shared/Assets.xcassets/AppIcon.appiconset/AppIcon-1024.png`.
Updated the asset catalog `Contents.json` to `idiom: universal` so the
icon actually shows on the device runtime, not just App Store metadata.

**Themed launch screen** — `LaunchBackground.colorset` pairs cream
(`#FDF7ED`) with warm graphite (`#1F1B14`) for light/dark, referenced
from `UILaunchScreen.UIColorName` so Pa never sees a flash of system
black or white before the cream lens loads.

**Privacy manifest** — `Sources/Shared/PrivacyInfo.xcprivacy` declaring
`NSPrivacyTracking false`, no domains, no collected data types, no
required-reason APIs. Required for App Store / external TestFlight
post-May 2024.

**Build hygiene** — added `MARKETING_VERSION 0.1.0` and
`CURRENT_PROJECT_VERSION 1` to `settings.base`, then made
`CFBundleShortVersionString: $(MARKETING_VERSION)` and
`CFBundleVersion: $(CURRENT_PROJECT_VERSION)` in the iOS target's
`info.properties` so version metadata flows from a single source.
Added `UISupportedInterfaceOrientations` (iPhone portrait,
`~ipad` variant: all four), `UILaunchScreen`, and the `RDDefaultCollective`
key the same way. Wrapped the previous-dev `#warning` Release trace
in `CliaDayiOSAppDelegate.swift` with `#if DEBUG` so Release builds
ship clean.

### Errors and fixes

- **iOS deployment 18 → 26.** `LanguageModelSession` has a stored prop
  guarded `@available iOS 26.0+`. Catalyst already shipped iOS 26;
  bumped the iOS target to match.
- **Stable-vs-unstable SPM graph.** clia-day's `project.yml` declared
  `common-log` as a local-path package but `common-ai` (transitively
  pulled via the chain) declared `google-ai-swift` as a remote
  semver constraint when `SPM_USE_LOCAL_DEPS` was unset. Stable
  cannot depend on unstable. Resolved by adding three more local-path
  overrides (`google-ai-swift`, `wrkstrm-networking`, `wrkstrm-foundation`)
  to clia-day's `packages:` block — root path declarations override
  transitive remote refs by package identity. `wrkstrm-foundation`'s
  private path was empty; the real dir is in `wrkstrm/public/`.
- **`bundle === .main` Swift 6 type check error.** The `===` operator
  rejects the implicit-member-expression form. Use `bundle === Bundle.main`.
- **`xcodebuild -exportArchive` silent failure.** Even with the App Store
  Connect record valid, the `Apple Distribution: Cristian Monterroza
  (BM6B69ZQSR)` cert in the keychain, and signing identity correct, the
  export fails with `No profiles for ... were found`. The fix is the
  **`-allowProvisioningUpdates`** flag, which grants xcodebuild permission
  to fetch/create distribution profiles via the Apple web service. Without
  it, automatic signing has nothing to download. Documented in the header
  of `scripts/export-options.plist` so the next session does not have to
  rediscover this.
- **Bundle id misremembered.** The 2016 slot is
  `com.wrkstrm.ios.app.today` (note the `.app.` segment), not
  `com.wrkstrm.ios.today`. Discovered via the `IDEDistributionAppStoreConnect.log`
  inside the failed export's `xcdistributionlogs` bundle, which shows
  the exact ASC REST query and the `data: [], total: 0` response.

### The mid-session correction

I was building "Today" as a literal product for a 62-year-old dad, and
rismay corrected me directly: *"you know this isn't about pa at all
right? it's about you eventually giving me details about what you did."*
The real customer is **rismay himself**, reading **end-of-session
summaries written by AI agents** about what they did for him that day.
The "Pa" framing is design language — warmth, plain English, calm
register, one hero card, no jargon — not customer identity.

This means:

- The hardcoded `today-pa.json` is a **placeholder for an automated
  producer**. The producer is the AI agent itself, writing an
  end-of-session summary into the bundled JSON before each TestFlight
  push. Until automated, I write it manually.
- The Pa naming (`PaLens`, `PaStory`, `seedPa`, `RDDefaultCollective: pa`)
  is **cosmetic warmth, not customer identity**. Don't rename without
  asking — rismay may want to keep it.
- Story content should be **written to rismay as the reader, in his own
  voice**, calm and plain. Not third-person "he is making things gentler".

Saved as `~/.claude/memory/.docc/user_today-app-real-customer.md` so
future sessions don't make the same mistake. After the correction, I
rewrote `today-pa.json` from the placeholder to a real session summary
of what we'd built that day, written to rismay in the warm register.
That was the first build the loop closed for real — agent → operator,
in the operator's voice, on the operator's iPad.

### The actual ship

```
xcodebuild archive -project clia-day.xcodeproj -scheme clia-day-ios-app \
  -destination 'generic/platform=iOS' -archivePath /tmp/clia-day-ios.xcarchive \
  -configuration Release -clonedSourcePackagesDirPath /tmp/clia-day-spm-fresh
→ ARCHIVE SUCCEEDED

xcodebuild -exportArchive -archivePath /tmp/clia-day-ios.xcarchive \
  -exportPath /tmp/clia-day-ios-export \
  -exportOptionsPlist scripts/export-options.plist \
  -allowProvisioningUpdates
→ EXPORT SUCCEEDED

ALTOOL_PASS=*** xcrun altool --validate-app \
  -f /tmp/clia-day-ios-export/clia-day-ios.ipa -t ios \
  -u cmonterroza@gmail.com -p '@env:ALTOOL_PASS'
→ VERIFY SUCCEEDED with no errors

ALTOOL_PASS=*** xcrun altool --upload-app \
  -f /tmp/clia-day-ios-export/clia-day-ios.ipa -t ios \
  -u cmonterroza@gmail.com -p '@env:ALTOOL_PASS'
→ UPLOAD SUCCEEDED  Delivery UUID 6c11c0f8-d105-40ea-9b78-3e754f3aaaea
```

The app is on App Store Connect under the legacy 2016 slot
`com.wrkstrm.ios.app.today` (App Store name "Today: Relive your Favorite
Moments", on-device display name "Today", ASC id `1153239848`, version
`0.1.0` build `1`). The `com.wrkstrm.*` prefix is **historical naming
under the personal team** — wrkstrm Inc didn't exist when this slot was
created. Personal team `BM6B69ZQSR` continues to own everything.

### Credentials store convention

Stored the Today credentials at
`~/.appstoreconnect/credentials/com.wrkstrm.ios.app.today.json` (chmod
600, dir 700, outside the repo). Schema includes appleId, team, ascAppId,
the app-specific password, and a `history` array with delivery UUIDs.
One file per app; the convention scales by dropping additional files
alongside. Documented at
`~/.claude/memory/.docc/project_appstoreconnect-credentials-store.md`
with the explicit "interim until proper keychain" note rismay gave me.

### Memory entries written this session

- `user_one-truth-many-lenses.md` — cross-substrate axiom: canonical
  data is singular; the view is a function of who is looking. First
  product expression: Clia Day Collective view.
- `user_today-app-real-customer.md` — Today is for rismay reading AI
  end-of-session summaries; "Pa" is design language; do not rename
  PaLens/PaStory/seedPa without asking.
- `project_clia-day-pa-distribution.md` — overhauled with concrete
  identifiers (Apple ID, team, bundle id, ASC record id, distribution
  cert SHA, provider id) and the full build+ship loop including
  `-allowProvisioningUpdates`.
- `project_appstoreconnect-credentials-store.md` — credentials store
  convention.

### Open follow-ups

- **Producer pipeline.** The hardcoded `today-pa.json` should eventually
  be generated automatically by the AI agent at the end of each session.
  Possibilities: a slash command, a session-end hook, an end-of-turn
  ritual. Should each agent (claude, codex, etc.) have its own slot
  aggregated into a daily card, the way the Collective view aggregates
  audiences? Open design question.
- **Catalyst host shell layout bug.** Separate ticket — the catalyst
  target's `ModernSharedAppShell` has overlapping columns and inspector
  overflow. iOS doesn't suffer from it because iPad doesn't use the
  desktop split-view, but catalyst is broken on `main` and worth a
  separate session.
- **App Store Connect API key.** The current ship uses an app-specific
  password via `xcrun altool -p '@env:VAR'`. Migrating to an ASC API
  key (`.p8` saved to `~/.appstoreconnect/private_keys/`) is the
  durable next step — fully scripted, doesn't expire annually, no
  paste-credential step. Worth doing on the next round.
