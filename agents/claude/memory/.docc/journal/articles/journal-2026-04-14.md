# 2026-04-14 - Prove koma work

## What shipped

- Built and ran 5 core komo end-to-end: `koma-echo`, `koma-scan-directory`,
  `koma-read-file`, `koma-git`, `koma-list`.
- All 39 tests pass across 4 packages (including on-device FoundationModels
  smart paths in echo and read-file).
- Fleet inventory via `koma-list`: 56 komo across 7 domains, 21 built, 11
  declared root koma. Core domain 100%.
- Rebuilt and launched koma-plant factory app.

## Decisions

- No "benchmark" koma exists on disk. Operator wants `koma-eval` created in the
  meta domain to back the existing Eval lane in koma-plant.

## Next steps

- Create `koma-eval` in meta domain.
- Wire koma-plant to live `koma-list` catalog (replace bundled fixture).
- Build out scaffolded komo in meta (`koma-classify`, `koma-need`) and build
  domains.
