# 2026-05-17

Long substrate session covering four major arcs.

## Arc 1 — Codex moment-v2 alignment

Operator caught me about to scaffold a `temporal-page-schemas` primitive
that would have collided with Codex's already-shipped
`timeline-moment-schemas v0.2.0` (commit `3724e86`). Audit-before-scaffold
discipline catching real drift in the moment.

Surfaced and refined:

- `vaults-org/private/universal/docc/tier-adaptive-typed-storage.architecture.docc/index.md`
  refined to cite Codex's actual primitives: `TimelineMomentIndexSegment` +
  `TimelineMomentLinkSet` + `compact-representation-schemas` +
  preservation-class enum (`ordinary/pinned/legal/sacred/benchmark/ephemeral`).
  Substantial rewrite of Read-First, Core Principle, Integrated
  Architecture diagram, Component Discipline Summary, StorageTierManager,
  BLIP Sync sections.
- Closed bead `schema-universal-temporal-page-primitive-scaffold` as
  superseded-by-prior-work (Codex commit 3724e86); detailed closure note
  citing the measured bit-budgets in Codex's own DocC.
- Refined beads
  `schema-universal-quantization-policy-primitive-scaffold` +
  `schema-universal-quantization-event-family-scaffold` to compose Codex's
  preservation-class enum + `CompactRepresentationPlan` instead of
  redefining policy categories.
- After Codex answered four asks (g enum inline; vocabulary coexist with
  TimeGranularity = schema name + TemporalQuantum = prose name; segment
  inlining is special case; schema-test-obligation-schemas is canonical
  proof-obligation contract), filed two new beads —
  `schema-universal-time-granularity-ordinal-shared-defs-refinement` +
  `schema-universal-timeline-segment-optional-compact-representation-plan-ref-refinement` —
  capturing the future refinements.

Commits: `5003ab3` vaults-org; `bea31bb219` mono pointer; auto-committed
bead edits in clia-org.

## Arc 2 — ghost.app voice cloning planning-ontology end-to-end walk

Operator's spark: *"this is one of those great ideas that is just banger:
ghost.app voice cloning. ideation to feature."*

The substrate's first full end-to-end test of the typed planning ontology
on a real banger feature.

**Substrate-coherence reveal** — voice clone isn't *a* ghost.app feature; it
is the DEFINING feature. Per `ghosts-as-substrate-top-level-peer` memory,
ghost = operator's typed projection at agent tier (ghost slug = operator
slug). The operator's voice is what makes the projection coherent. Mirrors
smoke-LoRA's two-path summon discipline (Apple Personal Voice native +
Swift-owned smoke-voice). Powers four typed consumers: narration,
agent-personality, sprite avatars, accessibility.

Authored typed records (all committed):

- **Exploration** (`exploration-ghost-app-voice-cloning`) — 7 typed
  decision-moments, 8 concepts, 2 hypotheses, 4 alternatives, 2 typed
  rejections with reversal conditions, status=2 (diverging).
- **PRD** (`prd-ghost-app-voice-cloning-v1`) — 7 goals, 6 success
  criteria, 5 non-goals, 8 functional requirements, 8 typed NFR concerns,
  4 user stories, 6 risks, 6 open questions, status=1 (drafted).
- **CUJ** (`cuj-ghost-app-summon-voice`) — "Operator SUMMONs voice into
  their ghost" walk: 10 modeled steps, 7 typed failure modes, 4
  forward-typed Swift Testing automated proofs, status=1 (modeled).
- **Materialization Plan** (`plan-ghost-app-voice-cloning-v1`) — authored
  as typed-prose markdown at `ghost-shell-org/.../plans/v1-voice-cloning/plan.md`
  when WorkstreamModel turned out to be the live-Discord-thread document,
  not the typed Plan record. 5 workstreams, 4 timeline gates, 8 closure
  criteria, 6 open-question gates. Plan + sibling
  `open-questions-for-operator-review.md` review-doc uncommitted at
  session end (preserved on disk).

Filed beads: `ghost-app-voice-cloning-specification-prd-scaffold`,
`ghost-app-voice-cloning-path-cuj-scaffold`,
`ghost-app-voice-cloning-materialization-plan-scaffold`,
`schema-universal-voice-clone-checkpoint-receipt-primitive-scaffold`,
`acting-docc-voice-cast-packet-authoring`,
`schema-universal-materialization-plan-primitive-scaffold` (the
substrate-gap-surfacing bead authored when WorkstreamModel turned out to
not be the typed Plan record).

Commits: `60236a6` + `665d95b` + `e58b53b` schema-universal; `7e9ce955` +
`d12a1e64` clia-org; `d4ade423ea` + `a600ddf9e1` + `a8719a908e` +
`e4d8847869` mono pointers.

## Arc 3 — Operator app canvas opening

Operator interrupted the voice-cloning Plan authoring with: *"where can
you raise these questions so I can review not through the terminal?"* and
then *"i was thinking about the operator app"* and finally *"we should
focus on our flagship operator app."* Pivot signal.

The terminal IS the wrong review surface; the agent's role is to make
typed substrate records readable BY THE OPERATOR through a non-terminal
lens. This is the substrate's `one-truth-many-lenses` doctrine becoming
load-bearing for the flagship app.

**Critical distinction surfaced**: Clia = traditional chat-with-agents
surface (industrial register). Operator = operator's substrate-grade
home (mythic register; flagship). Two different apps, two different
roles. **Operator app = pre-ship command center (planning ontology
phases 1-6); Presence app = post-ship monitor (phase 7)**. The split
maps onto the substrate's typed planning ontology pre/post-release
split.

IDEATION-mode painting (per the SOP authored later) over ~6 turns named
4 surfaces:

1. **Open Questions view** — substrate-wide aggregation of typed `oQ`
   arrays across all PRDs. Tonight's ghost.app PRD already has 6
   questions waiting.
2. **Apps × Rubric% × Stage view** — one canonical release rubric
   applied universally; two-axis (stage qualitative + percentage
   quantitative); operator's request: "we have a release rubric so we
   need to see that in percentage + stage terms".
3. **Open Threads view** — substrate-grade typed JOIN of beads
   (status:open) + commit log (filtered by agent attribution
   `cmonterroza+<agent>@wrkstrm.com`) + chronicle entries. Discord
   threads were glorious because messages = evidence; OpenClaw's work
   tracking was a lie because agent-narration ≠ evidence. The operator
   app's Open Threads view refuses agent-narrated state — typed
   Breakage indicator when claim, commit, and chronicle disagree on
   what happened.
4. **Provenance lens** — flippable per-panel transparency over what
   typed records + memories + chronicle entries + commits sourced any
   view. Substrate-grade radical transparency. Anti-lying surface.

5 typed primitives proposed (held UN-FILED pending canvas completion per
SOP discipline):

- `substrate-app-schemas.AppModel`
- `release-stage-schemas.ReleaseStageOrdinalityTable`
- `release-rubric-schemas.ReleaseRubricModel`
- `app-rubric-evaluation-schemas.AppRubricEvaluationModel`
- `release-stage-transition-schemas.ReleaseStageTransitionModel`

Canvas explicitly held open for next session. Operator's "let's keep
going with the operator app" is the active state.

## Arc 4 — Substrate-coherent ideation/creation SOP authored

Operator asked: *"i love these questions and how did you know to ask
them?"* — and after I answered (substrate memory + tonight's typed
records + operator's painting rhythm), said: *"we need to SOP this... i
like this for the ideation and creation phase."*

Authored `schema-universal/private/universal/substrate-coherent-ideation-and-creation.sop.docc/index.md`
as the substrate's first explicit operator-agent collaboration SOP.
Two-mode workflow:

- **Mode 1 (IDEATION)** — open-prose painting with substrate-sourced
  questions, held canvas, no typed records yet. Anti-patterns: premature
  scaffolding, agent-projected option menus, mid-ideation bead filing.
- **Mode 2 (CREATION)** — typed-models-first scaffolding with audit
  discipline, schema-family gap surfacing as beads, drift surfacing for
  operator yes/no, tight digikoma-git commits.
- **Modal-switch triggers** — operator-initiated only (`"b"`,
  `"do that actually"`, `"scaffold it"`, `"wait, i was thinking
  about..."`).

Composes with (does not replace) Codex's `property-survival.sop.docc/` +
`schema-family.sop.docc/` (schema-engineering procedure). Worked-example
canon section in the SOP itself draws from THIS session's actual moves.

Paired feedback memory saved at
`~/.claude/memory/.docc/feedback_substrate-coherent-ideation-and-creation-sop-active.md`
+ indexed in `MEMORY.md`. Future agent sessions will inherit the SOP
discipline at session-open.

Commits: `b2682b1` schema-universal; `e4d8847869` mono pointer.

## Lessons (substrate-grade, durable)

- **Audit-before-scaffold caught real drift in real time.** Codex's
  moment-v2 work shipped while my model was offline (between sessions);
  reading substrate state before scaffolding caught the collision before
  it landed.
- **The planning ontology validates itself by being used.** Authoring
  Spark → Exploration → PRD → CUJ → Plan on a real banger feature is the
  substrate's first concrete proof the ontology holds. Schema-family
  gaps surfaced (materialization-plan, monitor, closure) became
  beads instead of being papered over.
- **Operator-vs-Presence app split = pre/post-release split.** This is a
  substrate-coherent structural insight that names two flagship apps
  with non-overlapping roles, mapping onto the typed planning ontology
  phases.
- **Agent-narration ≠ evidence; typed records are the anti-lying
  surface.** OpenClaw's work-tracking was a lie because nothing typed
  pinned the claims to commits/files. The operator app's Open Threads +
  Provenance lens are the substrate's countermeasure.
- **Operator-coherent questions source from substrate memory, not
  projection.** The questions that landed well tonight were sourced from
  durable operator-authored memory files (`audit-not-eyeball-clone`,
  `typed-failure-epistemology`, `beads-as-agency-atoms`,
  `commit-plus-tag-attribution`, etc.). When the question feels
  surprisingly on-target, the substrate is doing the asking through the
  agent.

## Next-session starting points

- **Operator-app canvas is open** — 4 surfaces named, 5 typed primitives
  proposed un-filed. Resume painting (Chronicle scroll? Future-mirror
  panel? Agent roster + trust scores? Substrate health? SUMMON
  launcher?).
- **Ghost.app voice cloning is paused mid-construction-pending-gates** —
  6 open questions await operator review (provisional answers in
  `ghost-shell-org/.../plans/v1-voice-cloning/open-questions-for-operator-review.md`);
  `commercialRightsFlag` Bool-vs-ordinal drift flagged; the
  Materialization Plan + open-questions review-doc are uncommitted on
  disk.
- **Pending P1 bead**: `schema-universal-materialization-plan-primitive-scaffold`
  — the typed Plan family is the next substrate primitive to author when
  the operator returns to construction.
- **Codex collaboration is healthy** — four asks answered, two future
  refinement beads filed (`time-granularity-ordinal-shared-defs` +
  `timeline-segment-optional-compact-representation-plan-ref`); no
  blocking handoffs.
