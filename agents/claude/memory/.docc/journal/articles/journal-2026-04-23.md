# 2026-04-23: Sync + Network Discovery

Light session. Executed `$sync >h:hulk >a:claude` — reloaded operator
environment profile, rendered header with active S1 incident, persisted synced
agents JSONL.

Operator asked about shared computers on the network. Scanned local
Bonjour/mDNS via `dns-sd -B _smb._tcp local.` and discovered:

- **macMini** — advertising SMB on interfaces 26 and 23
- **Mac Pro** — advertising SMB on interfaces 26 and 23

Session ended before deciding next steps for the Mac Mini (build node, Codex
runner, repo sync, SSH setup).

---

## Session 2 — Organism resolution design (ghost-shell for agents)

Synced hulk + clia-org. Operator asked for "ghost-shell but for agents
in CLIA-ENV." Research across three parallel agents mapped:

- All 6 harnesses and their executables (hulk, codex, gemini, apple-pi,
  clide, pi)
- 3 organism types (ghost, koma, agent) and the `OrganismDoc.aspects`
  composition bag in organism-schemas v0.3.0
- Ghost-shell's 3-axis architecture (shells x configurations x protocols)
- CLIA-ENV's passive agent awareness (reads JSONL, no lifecycle)

Key correction from operator: organisms are the interchangeable part,
not harnesses. Ghost-shell, koma-plant, and Workflow should all accept
any organism type. The harness is fixed because it owns the executable.

Identified swift-agent-cli as the natural home for organism resolution —
already imports organism-schemas, doctor command decodes both agent and
organism kinds.

### Next fix

`swift-agent-cli v0.8 organism resolve --slug <slug> --path .` — scans
commissioned homes, decodes OrganismDoc, returns aspects bag and
protocol refs. Then wire CLIA-ENV to call it instead of flat JSONL
slug lookup.

## On deck

- Resolve macMini IP and check SSH / screen sharing availability
- Decide Mac Mini role in the substrate (build node, runner, sync target)
- Build `organism resolve` command in swift-agent-cli
- Wire CLIA-ENV header rendering to organism resolution
