@Metadata {
  @TechnologyRoot
  @PageKind(article)
  @PageColor(yellow)
  @TitleHeading("Claude's Creative Selection")
}

# Claude's Creative Selection

A typed kura-space curating Claude's creative-selection outputs —
narrative-shaped substrate-artifacts that synthesize what landed across
sessions in the substrate's symbolic-typed vocabulary.

Named by the operator 2026-05-31 at session-end: *"we can call this
your creative selection collection."*

## Provenance

This kura-space lives at:

```
private/universal/substrate/agents/claude/private/universal/kura-spaces/
  creative-selection/
```

Per the substrate's [[feedback_workflows-live-in-kura-spaces]] doctrine
(extended to apply to creative-selection outputs too), narrative
artifacts curated by an agent live in that agent's kura-space, not in
shared substrate tiers.

Composition with substrate-doctrine:

- The substrate-wide `creative-selection@wrkstrm.workstream` Swift
  Package describes the typed ideation workflow that all substrate
  ideation passes through. This kura-space is Claude-the-agent's
  variant: typed creative-selection OUTPUTS curated in narrative form,
  separate from the workstream's typed transition machinery.
- Per [[feedback_substrate-composes-typed-idea-molecules]] — pages here
  are typed-noun molecules composed from atom-level memory entries
  cross-references to commits, tests, schemas, and primitives.
- Per the operator's earlier-session pedagogy (symbolic-before-prose),
  pages use the substrate's symbolic vocabulary — emoji anchors, typed
  slugs, compressed prose — rather than natural-prose composition.

## What lives here

### 2026 H1 — substrate-typed process-spawning framework realignment

- <doc:2026-h1-the-ghost-who-learned-to-command-a-team>

A mid-year narrative covering the typed process-spawning framework's
end-to-end realignment: swift-subprocess 0.5 migration, typed
ProcessGroupSpec primitive family in common-process, the DDD-respecting
clia-ghost-runtime composition, the schema-family v0.2.0 bump, and the
5-workstream-Package CLI fleet retrofit. ~92 commits across 5 submodule
levels; 14 typed memory entries; 130 typed tests passing across the
realigned surface.

## How to add a page

A creative-selection page should:

1. Carry an `@Metadata { @PageKind(article) }` header (no
   `@TechnologyRoot` — that's reserved for index.md).
2. Compose from real substrate-typed records: commit refs, file paths,
   test outcomes, typed primitive slugs.
3. Honor the symbolic-typed-vocabulary discipline — emoji anchors are
   permitted and encouraged when each maps to a substrate-fact.
4. Be quotable as a standalone artifact (executive summary
   verifiable-by-cross-reference body + concrete deliverables).
5. Link back from this index via `<doc:slug>`.

## Topics

- <doc:2026-h1-the-ghost-who-learned-to-command-a-team>
- <doc:2026-06-03-the-iterm-boundary-and-the-cate-pivot>
