# cli-startup

Output produced by swift-harness-environment-cli startup run. This is generated content, not a file read.

**Command**: `swift-harness-environment-cli startup run --path .`
Reads .docc/index.md, renders header, pulls minimized mono profile.

**Phase total**: ~0 tok