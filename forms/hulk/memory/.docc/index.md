# Hulk Memory

@Metadata {
  @TechnologyRoot
}

DocC bundle for hulk's accumulated context.

## Structure

- `insights/` — durable architectural insights about hulks, the contract,
  and observed breaches
- `journal/` — chronological notes about hulk work
- `expertise/` — specialized knowledge about carrier shapes, memory bounds,
  process isolation, and failure modes
- `beats/` — short observation notes from working sessions
- `captures/` — raw pasted/cache artifacts promoted to stable source
  captures for traceability

The hulk's memory is about *being a hulk*, not about the agents it carries.
Agent-specific memory lives in the agent's commissioned identity, not here.

## Featured insights

- [Turn flow: default-agent mechanism](insights/turn-flow-default-agent-mechanism-2026-05-26.md) —
  observed shape of a typed-substrate-state turn (the default-agent
  re-forge case study). Three-layer model: native harness → agent
  active turn → supplements ∥ digikomas. Phase decomposition with
  per-phase supplement attachments + anti-patterns + cost-ledger
  consequences. Sourced for the substrate's supplement / digikoma
  factoring work.
- [Founding breach (carrier vs persona)](insights/founding-breach-2026-04-05.md) —
  the original separation between hulk-as-carrier and claude-as-persona.
  Substrate-canonical reference for any work that touches the
  carrier/persona boundary.
