---
name: creative-selection-runner-role-and-doctrine-refinement-pass
description: "Creative-Selection Runner role codified 2026-06-04 at substrate/roles/creative-selection-runner/. The role inherits Typed-Primitive Searcher's discipline + adds candidate generation, criterion authoring, doctrine-refinement-pass, and spawn-request assembly. Discovered NEW stage 'doctrine-refinement-pass' (cate doctrine → CodeEdit substrate-fit refinement; DraggableHandle NSResponder → cardDragGesture SwiftUI pattern) — bead-track promote to creative-selection.workflow-series.json v0.2.0."
metadata:
  node_type: memory
  type: feedback
  originSessionId: 2f6e8d5d-6c57-48a2-9fee-ab4870aa2a2f
---

**Role landed:** Creative-Selection Runner. The typed role an agent or operator enacts when running the creative-selection workflow series.

**Discovery (substrate-NEW):** the workflow series has a STAGE that wasn't yet named — the **doctrine-refinement-pass**. It sits between `opportunity-fit-review` and `spawn-request-assembly`. Pattern:

> After an initial candidate names the DOCTRINE the substrate needs, run a substrate-search to identify whether a substrate-shaped INSTANTIATION of that doctrine already exists. If yes, refine the pick. If no, the original doctrine-pick stands.

4 instances now confirmed (3 prior + 1 added 2026-06-04 evening):
- cate (doctrine = "substrate owns the renderer") → CodeEdit (substrate-shaped instantiation = SwiftTerm consumer sharing CommonConsoleKit dep)
- `TerminalEmulatorView` (lift target) → `CommonProcessTerminalView` (substrate-owned variant of `LocalProcessTerminalView`)
- `DraggableHandle` NSResponder (initial substrate-doctrinal NSResponder approach) → `cardDragGesture` SwiftUI pattern (substrate-shaped instantiation lifted from Architect)
- Apple macOS Tahoe Wallpaper `Spectrum / Random / Accent / named-color` dropdown (operator-surfaced prior art via screenshot) → substrate `AutoColorPolicy<Encoding>` + `TransitionKind` typed enums in `wrkstrm-color` (substrate-shaped instantiation of "policy decides which palette, when, with what transition function"). Same shape, substrate-typed, composes with the just-landed `glitchMix` cross-language primitive (Swift + JS, parity-verified to 6 decimal places).

**Canonical typed records (substrate-canonical truth — this memory is downstream pointer):**

- **Role:** `private/universal/substrate/roles/creative-selection-runner/private/universal/identity/creative-selection-runner.role-surface-manifest.json` — codifies x-required-disciplines, x-contributionMix, x-related-roles, x-axiom-citations, x-doctrine-refinement-pass extension.
- **Workflow execution instance:** `private/universal/substrate/collectives/spaces-universal/private/universal/kura-spaces/workflows/creative-selection/2026-06-04-concourse-session-execution.workflow-instance.json` — typed record of TWO arcs run this session (iterm-boundary-to-cate-pivot + concourse-naming-arc) with the doctrine-refinement-pass discovery documented in `discoveredStages[]` + four elaborationGaps flagged for bead-tracking.

**How to apply:**

- When running a creative-selection arc, look up the role surface manifest for the required-disciplines + contribution-mix budget.
- After the initial candidate is picked (opportunity-fit-review output), run the doctrine-refinement-pass: search the substrate for a more-substrate-shaped instantiation of the picked doctrine before assembling the spawn-request packet.
- The naming-shape variant (Concourse naming arc) used a CONDENSED form skipping source-and-claim-research because the constraint was substrate-internal. Future naming arcs can use the same condensed shape.

**Composes with:**

- [[lift-existing-patterns-not-reimplement]] — the discipline axiom the role inherits as its source-and-claim-research and doctrine-refinement-pass discipline.
- [[concourse-is-prototype-for-operator-app]] — the project context that frames why creative-selection-runner matters: it's the role that runs Operator.app's spawn cycle.
- [[2026-06-03-the-iterm-boundary-and-the-cate-pivot]] narrative — first arc receipt.
- [[cate-substrate-integration-2026-06-03]] investigation — first arc spawn-request assembly artifact.

**Bead-track followups (elaboration gaps the workflow-execution-instance flagged):**

1. Promote `doctrine-refinement-pass` to a typed workflow inside `creative-selection.workflow-series.json` v0.2.0.
2. Author a typed sub-workflow for the naming-shape condensed variant.
3. Author `SpawnRequestPacketModel` in a schema family — currently a referenced output but not typed.
4. Migrate execution records from separate `*.workflow-instance.json` files to `x-instances[]` entries inside the workflow.json itself (matches the wd canonical exemplar's pattern).
