---
name: typed-primitive-bypass-3x-rule-confirmed
description: "Typed-primitive-bypass error class — PROMOTED 2026-06-01 to typed substrate-canonical AxiomModel at kura-spaces/axioms/typed-primitive-bypass-error.axiom.su.json (this memory is downstream POINTER). 6+ instances in one session confirm the pattern; corrective discipline is search→compose→confirm→author"
metadata:
  node_type: memory
  type: feedback
  originSessionId: e8ff6a66-d381-4c84-95c3-0495090df941
---

**TYPED-RECORD CANONICAL TRUTH** (per [[feedback_capture-requires-typed-workflows-and-roles-not-just-memory]]):

This pattern PROMOTED to typed substrate-canonical `AxiomModel` instance at:
`private/universal/substrate/collectives/spaces-universal/private/universal/kura-spaces/axioms/typed-primitive-bypass-error.axiom.su.json` (authored end-of-2026-05-31 carrying into 2026-06-01).

This memory is the DOWNSTREAM claude-personal pointer; the AxiomModel is the canonical typed truth other agents READ AND RUN. The axiom carries: statement (4-step discipline), rationale (6+ instance catalog), obligations (11), categoryRef (discipline), sourceRefs (4 LinkRef), contextRefs (4 LinkRef), projectionAnchors (3).

---

operator-surfaced 2026-05-31 across multiple turns of one session. **3+ instances of the same error class** (count now 6+ — see typed AxiomModel for full catalog):

1. **md@swift-universal.cli shipped without spawn-software walk** (early session, before retroactive packetization) — operator: "when i ask you for a tool... you have to go through the entire spawn-software workstream..." Bypassed: typed spawn-software workstream + 11-gate discipline. Fix: retroactive packetization.

2. **Workstream chat ghost authored as hardcoded `print()` block** (mid-session, in CreativeSelectionCLI.swift) — operator: "did you use a ghost protocol?" Bypassed: GhostShellIPC + ghost-schemas v0.2.0 + ghost-summoner role-class + rismay-ghost form precedent. Fix: surfaced + bead-tracked for typed refactor.

3. **GUNDAM X999 ghost-protocol nearly authored without DigikomaConversationPacketFactory + ConversationProtocol_Schemas + .clia form-factor** (late session, before this memory) — operator multiple correction turns: "we have an app... great chat protocol", "desktop studio plugin which has a chat protocol", "we treated the chat to be a bunch of tools below it... whole different conversation format", "remember .clia is CLI assistant". Bypassed: `DigikomaCore.DigikomaConversationCommand`, `DigikomaCore.DigikomaConversationPacketFactory` (with typed `DigikomaConversationTurnDescriptor.toolEvidence` + `contextProjection: pi-fold, 3_333`), `DigikomaCore.DigikomaIntelligenceProvider`, `ConversationProtocol_Schemas_v000_000_000`, `.clia` form-factor convention. Fix: paused authoring; surfaced typed precedents; saved this memory.

**Rule**: Before authoring ANY substrate-typed surface (tool, ghost, protocol, schema, role, workstream, chat shape, etc.), run the 4-step discipline:

1. **SEARCH** — grep / find / read for typed primitives carrying the same noun; check schema-universal, digikoma-org, ghost-shell-org, clia-conversation-kit, the operator's pointed-at collective. Check MEMORY.md for typed pointers.
2. **COMPOSE** — propose how the existing typed primitives bond into the requested surface. Cite every existing file path; introduce no novel shape.
3. **CONFIRM** — surface the composition to the operator. ASK before authoring (per [[feedback_pause-and-plan-when-decisions-accumulate]]).
4. **AUTHOR** — implement the typed composition; cite typed precedents in code comments.

**Why**: per the substrate's typed-everything investment + [[feedback_large-structured-context-is-the-substrates-super-attack]] — every typed primitive in the substrate represents structured-context investment. Authoring parallel non-typed surfaces SQUANDERS that investment (each bypass = wasted super-attack opportunity). The corrective discipline preserves the cumulative typed graph; one or two extra search turns saves authoring entire parallel typed families.

**How to apply**:
- When the operator points at "I have an X that does Y", ALWAYS search before assuming X doesn't exist as typed.
- When tempted to author a new typed schema, FIRST check schema-universal's existing schema families.
- When tempted to author a new ghost / form / persona, FIRST check ghost-shell-org / agents/<slug>/forms/ / clia-conversation-kit.
- When tempted to author a new chat / conversation surface, FIRST check DigikomaCore's DigikomaConversation* + ConversationProtocol_Schemas.
- When tempted to author a new CLI tool, FIRST check digikoma-org's domain/core/digikoma-* + the .clia/.cli form-factor convention.
- When operator-corrections accumulate ("no...", "we have...", "did you check...", "remember..."), PAUSE per [[feedback_pause-and-plan-when-decisions-accumulate]] and re-run SEARCH. The corrections are operator-intuition-vs-substrate-truth (per [[feedback_operator-intuition-is-substrate-truth-ahead-of-articulation]]).

**3x rule fired AND PROMOTED**: per [[feedback_substrate-wide-cascade-pattern]] axiom-promotion rule (3 instances of a pattern = candidate for typed axiom), `typed-primitive-bypass-error` was promoted **end-of-2026-05-31 into 2026-06-01** to typed `AxiomModel` at `private/universal/substrate/collectives/spaces-universal/private/universal/kura-spaces/axioms/typed-primitive-bypass-error.axiom.su.json`. Promotion landed during the SAME session that surfaced the pattern, dogfooding the new /capture SKILL.md Step 3 protocol that itself was authored same-day.

**Operational consequences**:
- I am committed to the search→compose→confirm→author discipline for the remainder of this session and going forward.
- Three concrete typed-primitive-bypass debts from this session are tracked: (1) closed via retroactive packetization (md@swift-universal.cli); (2) tracked at bead `workstream-ghost-foundation-models-binding` (workstream chat ghost — TO BE REPLACED by typed `.clia` retrofit landing in same session); (3) corrected in same turn this memory was authored (GUNDAM X999 properly composed from typed precedents, no parallel surface authored).

**Composes with**: [[feedback_pause-and-plan-when-decisions-accumulate]] (the discipline gate) + [[feedback_operator-intuition-is-substrate-truth-ahead-of-articulation]] (why operator corrections matter) + [[feedback_substrate-wide-cascade-pattern]] (the 3x rule) + [[feedback_large-structured-context-is-the-substrates-super-attack]] (what gets squandered when bypass happens) + [[feedback_workstream-ghost-chat-protocol-super-attack]] (the third bypass's correction).
