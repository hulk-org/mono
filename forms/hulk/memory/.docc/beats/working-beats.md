# ^claude: Working Beats

Active beat ledger for `claude`.

## Current beats

- Commissioned home exists and is ready for explicit `$sync >claude`.
- Primary orientation route is `claptrap.docc` -> `acting.docc` -> OpenClaw.
- `apple-pi-bench-press` tool explorer is built. 36 FM tools are cataloged,
  3 are currently invocable.
- Beat dedup bug is fixed. Next: canonical fixtures + expected results.
