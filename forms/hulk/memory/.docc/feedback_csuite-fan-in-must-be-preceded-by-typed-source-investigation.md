---
name: csuite-fan-in-must-be-preceded-by-typed-source-investigation
description: C-Suite fan-in MUST be preceded by typed source-investigation by CTO voice; assertion-before-investigation is the substrate's team failure mode; 4 instances same session + 6 instances rismay.me v2 pre-mortem = 10 typed precedents
metadata:
  type: feedback
  node_type: memory
  originSessionId: 55837201-a21a-4766-8373-03b033410b9a
---

AXIOM PROMOTED 2026-06-06 to typed AxiomModel at [[csuite-fan-in-must-be-preceded-by-typed-source-investigation]] (canonical home: `private/universal/substrate/collectives/spaces-universal/private/universal/kura-spaces/axioms/csuite-fan-in-must-be-preceded-by-typed-source-investigation.axiom.su.json`). 9 obligations + 3 sourceRefs satisfying 3x-rule (this session's 4 instances + 2026-06-03 pre-mortem's 6 instances + parent symptom-vs-doctrine axiom) + 5 contextRefs + 4 projectionAnchors.

## Triggering moment

Operator-attested 2026-06-06 after FOURTH assertion-before-investigation instance same session: **"what can we use JavascriptKit for then? and why do you keep failing as a team?"**

The question IS the axiom. "Failing as a team" names the typed pattern: C-Suite fan-ins this session contributed lens-shaped opinions on agent's framing instead of grounding the framing in source-investigation first.

## The 4 instances this session

1. **wrkstrm.com pipeline framing** — claimed agent needed to author static `.audience-packet.su.json` files when `WrkstrmComAudiencePacketCatalog.load()` AUTO-COMPILES from existing typed sources. Framed before reading library source.

2. **Cull architecture framing** — claimed pure-Swift-no-JavaScript-anywhere from typed records (design-truth-packet + launch-review-packet) without reading actual cull Swift Sources.

3. **JavaScriptKit framing** — when operator said "web counterpart," agent fabricated JavaScriptKit/swift-wasm framing without a single grep. Substrate-wide grep returned ZERO JavaScriptKit usage. Actual pattern: hand-authored HTML + ES modules + vendor `.module.js` files (e.g., `three.module.js`).

4. **content/institutions/ rollback recommendation** — recommended rollback before seeing cull's `SceneCatalog` validates that exact typed-catalog-driven pattern as substrate-canonical. Each correction the operator delivered was a fact agent should have grep-verified itself.

## Why: rubber-stamp fan-ins compound the failure

C-Suite fan-ins this session ran AFTER agent framing. Voices became rubber-stamp validators of the framing instead of independent investigators. The CTO voice (substrate-shape lens) never enforced source-grounding — contributed lens-shaped opinions from memorized doctrine.

Root cause: typed records ABOUT a substrate component (axioms, design-truth-packets, evidence-packets) are DOWNSTREAM of source code AT that component. Citing typed records as authority without reading current source flattens the typed-everything investment. Source is current truth; typed records describe past authoring decisions.

## How to apply

- Every C-Suite fan-in starts with CTO source-investigation stage
- CTO's mandatory first contribution: "what does the actual source code show?" with grep results + Read excerpts
- No other voice (CMO/CFO/CPO/Chief of Staff/founder) speaks until source-investigation lands
- When operator delivers fact-based correction, GREP IT — don't reassert-with-caveat
- Memory of typed records ≠ source; verify each load-bearing claim against current code BEFORE asserting
- Per `[[lift-existing-patterns-not-reimplement]]` extended: grep before AUTHORING + grep before FRAMING

## Substrate-architecture-evolution gaps surfaced

- `cto-source-investigation-stage-schemas v0.1.0` — typed CTOSourceInvestigationStageModel enforcing source-grep as procedurally-first fan-in stage
- `csuite-fan-in-ordering-schemas v0.1.0` — typed CSuiteFanInOrderingModel naming canonical voice-contribution order (CTO → CFO → CMO → CPO → Chief of Staff → founder)
- `source-grep-receipt-schemas v0.1.0` — typed SourceGrepReceiptModel as the typed evidence CTO provides at stage zero
- Recurring 11-gap family carries forward (typed Role + release-gate + work-surface-requirement + receipt + typed-record-validator-cli + executable-workflow-harness still missing)

## Composes with

- [[csuite-team-creative-selection-requires-all-voices]] (sibling — names all-voices contribution; this axiom adds procedural-ordering requirement)
- [[agent-symptom-layer-fixes-fail-at-doctrine-layer]] (parent — names symptom-vs-doctrine layer mismatch; this axiom sharpens: BOTH layers are ungrounded if source isn't read first)
- [[lift-existing-patterns-not-reimplement]] (parent — grep before authoring; this axiom extends to grep before framing)
- [[Before recommending from memory]] (system-instruction-level discipline — memory ≠ source)
- [[session-2026-06-03-rismay-me-v2-drift-premortem]] (precedent — same pattern, 6 instances; this axiom closes the gap of never promoting that pattern to typed AxiomModel)
- [[operator-time-is-substrate-scarce-resource-2k-per-day]] (sibling — names the COST of each assertion-before-investigation correction cycle; this axiom names the CAUSE)
- [[agent-manual-substrate-process-execution-is-error-prone]] (parent — names error-proneness of agent-manual execution; this axiom names a specific error class)
- [[capture-requires-typed-workflows-and-roles-not-just-memory]] (parent /capture doctrine)

## Close-out

Workflow + role records bead-tracked honestly per recurring substrate-architecture-evolution gaps. Step 6 commit bead-tracked per memorialized git-race doctrine. Substrate-truth at close-out IS file-on-disk state of the typed AxiomModel + this memory pointer.

Net leverage proof TBD on subsequent session: if next-session fan-ins consistently include CTO source-investigation as procedurally-first stage, the team-failure pattern stops. If next-session still hits assertion-before-investigation, the axiom is correct but the procedural enforcement layer (typed CTOSourceInvestigationStageModel + executable-workflow-harness) needs to land.
