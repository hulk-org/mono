---
name: uptobat operator is John Johns
description: substrate operator slug `uptobat` (👻 emoji, lane-governance, discord taxonomy) maps to the human John Johns; NOT `johnwhitecastle`
type: project
originSessionId: 40fbc6fc-eb17-423d-89ce-0ac679ccd6b9
---
The substrate operator slug `uptobat` resolves to the human **John Johns**.
The typed identity at `private/universal/substrate/operators/uptobat/private/universal/identity/uptobat@rismay.substrate.identity.json` does NOT name "John Johns" anywhere — it carries `displayName: "Uptobat"`, emoji 👻, tags `operator,collaborator,lane-governance,discord`, and a purpose around operator-lane privacy + Discord taxonomy. The John-Johns identification lives in rismay's head, not in the JSON.

Do NOT confuse with `johnwhitecastle` (different person — also rismay-substrate operator slug, displayName "John Whitecastle," emoji 🏰, terminal-design-system style). "John" by itself in rismay's chat about agents/operators typically means John Johns = uptobat, not John Whitecastle.

The 👻 ghost emoji aesthetic and lane-governance role on uptobat is the substrate-shape of what rismay sometimes calls the "ghostty operator" — `ghostty` is the *aesthetic*, `uptobat` is the canonical operator slug.

**Why:** When rismay says "John" in conversation about agents, identity, or projection (e.g., allora's edge-inferred operator), he means John Johns = uptobat. Confusing this with johnwhitecastle leads to wiring the wrong operator slug into organism roles and identity bundles.

**How to apply:** For any agent that names John as an operator/referent (especially projection agents like allora), encode the operator slug as `uptobat`, not `johnwhitecastle`. The display/human name "John Johns" goes in `notes[]` or `extensions.operators[].displayName` on the typed identity; the slug stays `uptobat`.
