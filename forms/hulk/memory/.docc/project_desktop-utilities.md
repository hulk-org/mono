---
name: Desktop Utilities app
description: Renamed from desktop-by-wrkstrm; slug desktop-utilities, bundle me.rismay.desktop-utilities; transparent overlay + configurable z-order desktop widget layer
type: project
---

**Desktop Utilities** — slug `desktop-utilities`, bundle `me.rismay.desktop-utilities`.

Renamed from `desktop-by-wrkstrm` (2026-04-17). Drop the `-by-wrkstrm` suffix.

Product: configurable desktop widget layer with z-order flexibility — widgets
can sit below desktop icons or float above all windows. First widget:
Interview Clock with transparent overlay mode (click-through, opacity slider
5–50%, targets NSScreen.main).

**Why:** practical everyday utility, not a branded product. Apple-system naming
register ("Desktop Utilities" like "Disk Utility").

**How to apply:** use `desktop-utilities` as the canonical slug in all new
references. Rename from `desktop-by-wrkstrm` is pending — current code still
uses the old name. App lives at
`wrkstrm-app/private/apple/apps/desktop-by-wrkstrm/` until rename is executed.
