---
name: session-2026-06-04-substrate-plugin-system-for-ai-models
description: "Session capture for the 2026-06-03/2026-06-04 arc establishing substrate's typed plugin system for AI models — codex boot recovery → maintainers/-tier doctrine → first family + classification surface + typed CLI. Pointer to typed workstream + workflow + axiom records."
metadata:
  node_type: memory
  type: project
  originSessionId: 48bb3ae1-1a94-4651-ac23-1a617d8ef0d2
---

Session captured 2026-06-04 under `/capture` with directive "make sure this goes up to workstreams all the way down." Single-session arc from codex boot failure (self-loop symlink) → substrate-doctrinal vertical slice for plug-system contract authoring.

**Why:** Operator caught codex not booting; root cause was `chatgpt/forms/loom/agents/loom -> ..` self-loop breaking codex 0.134.0's tier-1 walker. Recovery expanded into authoring substrate's first maintainers/-tier schema family, the doctrinal SOP for it, the classification surface, and a typed Swift CLI for symlink operations that replaces every future bash find/readlink dance.

**How to apply:** This memory entry is a DOWNSTREAM POINTER per `[[capture-requires-typed-workflows-and-roles]]`. The canonical truth lives in the typed substrate-canonical records cited below — read those when another agent needs to execute the same workstream shape, enact the substrate-integrity-steward role, or apply the partition axiom.

## Typed records authored this session (canonical truth — read these, not this memory)

- Workstream — `[[substrate-plugin-system-for-ai-models]]` at `private/universal/substrate/collectives/wrkstrm/private/universal/kura-spaces/workstream/formula/spawn-software/runs/substrate-plugin-system-for-ai-models.workstream.json` — the umbrella for the entire session arc, references the workflow + axiom + role below
- Workflow — `[[maintainers-tier-schema-family-authoring]]` at `private/universal/substrate/collectives/spaces-universal/private/universal/kura-spaces/workflows/maintainers-tier-schema-family-authoring/v0.1.0/maintainers-tier-schema-family-authoring.workflow.json` — 11-stage workflow from doctrine SOP through JSON Schema through Swift Codable to kura-space tree; 4-category error taxonomy; 1 concrete instance recorded
- Axiom — `[[substrate-agent-ness-does-not-override-partition]]` at `private/universal/substrate/collectives/spaces-universal/private/universal/kura-spaces/axioms/substrate-agent-ness-does-not-override-partition.axiom.su.json` — PROMOTED this session via 3 same-session confirmations (openclaw real-dir target / openclaw schema-family placement / pre-existing substrate-wide maintainers/openclaw/ tier)
- Role cited — `[[substrate-integrity-steward]]` at `private/universal/substrate/roles/substrate-integrity-steward/private/universal/identity/substrate-integrity-steward.role-surface-manifest.json` — the role enacted heavily this session (symlink hygiene, harness CLI repair, routing-table cleanup); pre-existing, not re-authored
- Doctrine SOP — `[[maintainers-tier-doctrine]]` at `private/universal/substrate/collectives/schema-universal/.docc/maintainers-tier-doctrine.md` — the per-domain maintainers/<org>/ partition for typed external-format mirrors; referenced from .docc/index.md Structure + Read next

## Concrete artifacts beyond typed records

- First maintainers/-tier schema family: `domain/ai/maintainers/openclaw/schema-families/openclaw-models-config-schemas/v0.1.0/` (JSON schema + SPM package build green + 4 Swift Codable sources + version enum)
- Root kura-spaces tree: `private/universal/kura-spaces/ai-models/{openai,openclaw,ollama}/` with index.md per provider
- spaces-universal AI Models kura: 4 AIModel canonical records covering anthropic-claude, openai-codex, apple-foundation, meta-llama (cross-family classification surface)
- `[[symlinks@swift-universal.cli]]` — typed Swift CLI for substrate symlink operations (library + 3 AsyncParsableCommand subcommands: list / find-loops / audit); converts session's bash dance into permanent typed primitive

## 3x-rule axioms promoted this session (in typed AxiomModel)

- `[[substrate-agent-ness-does-not-override-partition]]` — operator caught initial draft including a "Special Case — Substrate Agents With Legacy Config Formats" carve-out for openclaw; clean doctrine: partition criterion is WHO AUTHORED THE TYPED CONTRACT, not WHO RUNS THE FILE. 3 same-session confirmations met the rule.

## 3x-rule axiom candidates bead-tracked (need 1-2 more confirmations OR explicit typing)

- `exploration-is-substrate-tooling` — symlinks CLI is this session's concrete proof; pattern has 3+ historical instances already. Worth promoting next /capture.
- `plug-system-host-contract-vs-plugin-manifest` — new doctrine from this session's codex-rs-vs-canonical comparison. Needs 1-2 more confirmations + AxiomModel authoring.
- `layered-symlink-drift` — climb-count + base-dir layers; 2+ confirmed this session (clia + spark). Needs 1 more for promotion.
- `typed-pointer-by-slug-not-ordinal` — substrate-scheme `ai-model-family://<slug>` pattern. Implicit; worth explicit typing alongside the `identity://<slug>` precedent.

## Operator quotes worth preserving (the why behind the records)

- "i think the symlinks are not really adjusting well with his new chatgpt/forms/codex harness self" — opened the session
- "these are not part of our families...." — caught the openclaw special-case carve-out, triggered the axiom
- "eeeexactly. we use like a link ref 0.3 or discriminator" — locked the LinkRef v0.3.0 substrateScheme as the canonical plug→host pointer shape
- "we really need a symlinks@swift-universal.cli which can maybe use swift directory tools search methods..." — triggered the substrate-tooling extraction
- "make sure this goes up to workstreams all the way down" — invoked this /capture at the workstream tier

## Cross-references to existing substrate doctrine applied this session

Composes with: `[[capture-requires-typed-workflows-and-roles]]` + `[[do-not-break-domain-driven-design]]` + `[[content-lives-in-its-owners-home]]` + `[[typed-primitive-bypass-3x-rule-confirmed]]` + `[[deferral-is-drift-do-it-now]]` + `[[exploration-is-substrate-tooling]]` + `[[Always run SwiftJSONFormatterCLI on JSON writes]]` + `[[No bash scripts either — only saved Swift CLIs]]` + `[[async-parsable-command-is-axiom]]` + `[[substrate-typed-discriminator-pattern]]` + `[[ordinality-table-entries-immutable-once-released]]` + `[[savepoint-daemon-races-your-commits]]`.
